version: 2

models:
  - name: int_acceptance_rate_unnested
    columns:
      - name: external_ref
        test:
          - not_null
          - unique
      - name: exchange_rate
        test:
          - not_null
